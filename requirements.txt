discord.py[voice]>=2.7,<3  # Persistent UI and current Pin Messages permission
python-dotenv>=1.0
python-a2s>=1.4          # optional: A2S liveness fallback when logs go stale
Pillow>=11.3,<13        # in-memory rank cards and bundled fonts
resvg-py>=0.5,<0.6      # offline SVG templates; native wheels, no Cairo needed
